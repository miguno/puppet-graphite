#
# This file is managed by Puppet.
#

mode = 'django'
bind = '127.0.0.1:<%= scope.lookupvar('graphite::web::gunicorn_port') %>'
pythonpath = '<%= scope.lookupvar('graphite::params::graphite_pythonpath') %>'
preload_app = True
workers = <%= scope.lookupvar('graphite::web::gunicorn_workers') %>
timeout = <%= scope.lookupvar('graphite::web::gunicorn_timeout') %>
django_settings = 'settings'
